package org.terrevivante.tvjournalists.domain.model;

/**
 * Rôles métier disponibles pour contrôler les permissions dans l'application.
 */
public enum Role {
    ADMIN,
    THEME_MANAGER,
    USER
}
